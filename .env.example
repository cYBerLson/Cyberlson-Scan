# Flask secret key for session management and CSRF protection.
# MUST be a long, random string in production.
SECRET_KEY=

# Flask configuration environment. Options: 'development', 'production'.
# Set to 'production' for deployment.
FLASK_CONFIG=development

# Port for the Flask application to run on. Default is 5000.
# PORT=5000
